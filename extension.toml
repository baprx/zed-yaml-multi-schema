id = "yaml-multi-schema"
name = "YAML Multi-Schema"
version = "0.6.2"
schema_version = 1
authors = ["Baptiste ROUX"]
description = "Per-block JSON Schema validation and completion for YAML files."
repository = "https://github.com/baprx/zed-yaml-multi-schema"

[language_servers.yaml-multi-schema]
name = "YAML Multi-Schema LSP"
languages = ["YAML"]
