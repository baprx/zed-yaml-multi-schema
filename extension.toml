id = "zed-yaml-multi-schema"
name = "Zed YAML Multi-Schema"
version = "0.3.0"
schema_version = 1
authors = ["Zed YAML Multi-Schema contributors"]
description = "Per-block JSON Schema validation and completion for YAML files (Helm umbrella mode)."
repository = ""

[language_servers.zed-yaml-multi-schema]
name = "Zed YAML Multi-Schema LSP"
languages = ["YAML"]
